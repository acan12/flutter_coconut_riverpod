import 'package:flutter/material.dart';

class UIWidgetscreen extends StatelessWidget {
  const UIWidgetscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('UI Widgets')));
  }
}
